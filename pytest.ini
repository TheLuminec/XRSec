[pytest]
pythonpath = .
testpaths = tests
python_files = test_*.py
markers =
    unit: Fast, isolated unit tests.
    integration: Integration tests that may rely on local datasets/checkpoints.
    slow: Long-running tests.
